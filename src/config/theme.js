/**
 * @file theme.js (LEGACY)
 * @deprecated Use `src/theme/index.js` for Chakra UI v3 system configuration.
 * This file is kept for backward compatibility with documentation references only.
 */
import { system } from "../theme/index";

export default system;
