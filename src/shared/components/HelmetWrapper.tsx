/* eslint-disable */
import React from "react";

/**
 * @component HelmetWrapper
 * @description No-op wrapper for Next.js 15 migration.
 * SEO is now managed via Next.js Metadata API in layout/page files.
 */
const HelmetWrapper = (props: any) => {
  return null;
};

export default HelmetWrapper;
