# 📖 Glosario Técnico (Glossary)

## A
- **API (Application Programming Interface):** Conjunto de reglas que permite que el Frontend se comunique con el Backend.
- **Atomic Design:** Metodología de diseño que divide interfaces en Átomos, Moléculas y Organismos.

## C
- **Chakra UI:** Librería de componentes React accesible y modular que usamos para construir la UI.
- **CMS (Content Management System):** Sistema para gestionar contenido web. Planeamos migrar a un "Headless CMS" en el futuro.
- **Componente:** Pieza de código reutilizable que renderiza una parte de la interfaz (ej. `Button`, `Card`).

## F
- **Feature-Based Architecture:** Arquitectura donde el código se agrupa por funcionalidad de negocio (ej. `projects/`, `services/`) en lugar de por tipo de archivo.
- **Firebase:** Plataforma de Google que provee nuestra infraestructura (Hosting, BD, Functions).
- **Firestore:** Base de datos NoSQL en tiempo real de Firebase.

## G
- **Glassmorphism:** Estilo visual caracterizado por efectos de transparencia, desenfoque (blur) y bordes sutiles, imitando vidrio esmerilado.

## H
- **Hook (React):** Función especial que permite "enganchar" características de React (como estado) en componentes funcionales. Ej: `useState`, `useReclamoForm`.

## L
- **Lazy Loading (Carga Perezosa):** Técnica de optimización que retrasa la carga de imágenes o componentes hasta que son necesarios, mejorando la velocidad inicial.

## R
- **React:** Librería de JavaScript para construir interfaces de usuario.
- **Resend:** Servicio API externo utilizado para enviar correos electrónicos transaccionales de alta entregabilidad.

## S
- **Serverless:** Modelo de ejecución donde el proveedor de nube (Google) gestiona dinámicamente la asignación de recursos. Nuestras funciones de backend son "serverless".
- **SPA (Single Page Application):** Aplicación web que carga una sola página HTML y actualiza el contenido dinámicamente, sintiéndose como una app nativa.
