# Métricas baseline

Guardar aquí la salida de: `lint.txt`, `typecheck.txt`, `test.txt`, `build.txt`, `env.txt` y subcarpeta `lighthouse/`.

> Los archivos binarios/pesados de esta carpeta NO se versionan (ver `.gitignore`). Solo los `.md` se trackean.
